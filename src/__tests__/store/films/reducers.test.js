import { reducers } from '../../../store/modules/films'

describe('films reducers', function () {
  describe('loadFilms', function () {
    describe('on a SUCCESS loader action', function () {
      it('should set films on the store', function () {
        // todo
      })
    })
  })
})
